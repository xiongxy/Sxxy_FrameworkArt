@model Sxxy_FrameworkArt.Web.ViewModels.System.SystemMenuViewModels.SystemMenuListViewModel
@using (Bootstrap.PageLayout_BeginRow())
{
    using (Bootstrap.PageLayout_BeginColumn())
    {
        using (Bootstrap.SearcherPanel(x => x, Model.ViewModelId))
        {
            @Bootstrap.TextField(x => x.Searcher.ActionName)
        }
    }
    using (Bootstrap.PageLayout_BeginColumn())
    {
        @Bootstrap.TreeTableFor(x => x)
    }
}